# Deque Buffer Ring

The ring-backed `Deque` columns — front door, constructors, and per-column `push`/`clear`/`reserve`/`clone` over `Buffer.Ring`, `Buffer.Ring.Bounded`, and their `Ownership.Shared` wrappers — extracted from `swift-deque`.
